# Yarn Classic (1.22) settings.
# Upgrade path to Yarn Berry (4.x) once corepack is available on this machine:
#   corepack enable && yarn set version stable && echo 'nodeLinker: node-modules' > .yarnrc.yml
--install.frozen-lockfile false
workspaces-experimental true
