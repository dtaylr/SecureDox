apiVersion: 1

providers:
  - name: SecureDox
    orgId: 1
    folder: SecureDox
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
